# Vulas
VULAS_RELEASE=3.0.14-SNAPSHOT
VULAS_ENV=prod

# *** MANDATORY SETTINGS ***
# PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=

# Spring (should be equal to the values in PostgreSQL section above)
spring.datasource.username=
spring.datasource.password=

# *** OPTIONAL SETTINGS ***
# Jira (optional)
JIRA_USER=
JIRA_PASSWORD=

# Proxy
HTTP_PROXY_HOST=
HTTPS_PROXY_HOST=
HTTP_PROXY_PORT=
HTTPS_PROXY_PORT=
NON_PROXY_HOSTS=
http_proxy=
https_proxy=
no_proxy=
