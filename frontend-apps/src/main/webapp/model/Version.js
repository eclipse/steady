jQuery.sap.declare("model.Version");

// Placeholders replaced during Maven build
model.Version = {
		version:"$version$",
		buildTimestamp:"$buildTimestamp$",
		buildNumber:"$buildNumber$",
		buildBranch:"$buildScmBranch$"
};
